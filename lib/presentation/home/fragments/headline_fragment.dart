import 'package:flutter/material.dart';

class HeadlineFragment extends StatelessWidget {
  const HeadlineFragment({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Headline Fragment"),
    );
  }
}
