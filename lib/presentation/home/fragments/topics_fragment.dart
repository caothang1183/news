import 'package:flutter/material.dart';

class TopicsFragment extends StatelessWidget {
  const TopicsFragment({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Topics Fragment"),
    );
  }
}
